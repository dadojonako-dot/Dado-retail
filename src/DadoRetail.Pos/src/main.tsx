import React,{useState} from 'react';import{createRoot}from'react-dom/client';import'./pos.css';
type Item={id:number;name:string;qty:number;price:number};
function App(){const[barcode,setBarcode]=useState('');const[items,setItems]=useState<Item[]>([{id:1,name:'Вода ДАДО 1.5 л',qty:2,price:12},{id:2,name:'Хлеб белый',qty:1,price:5.5},{id:3,name:'Молоко 1 л',qty:1,price:14}]);const total=items.reduce((s,x)=>s+x.qty*x.price,0);const scan=()=>{if(!barcode)return;setItems(v=>[...v,{id:Date.now(),name:`Товар ${barcode}`,qty:1,price:0}]);setBarcode('')};return <main><header><b>ДАДО</b><span>Продажа · Касса 1</span><aside>Кассир: admin</aside></header><section className="layout"><div className="sale"><div className="scan"><input autoFocus value={barcode} onChange={e=>setBarcode(e.target.value)} onKeyDown={e=>e.key==='Enter'&&scan()} placeholder="Сканируйте штрихкод или найдите товар…"/><button onClick={scan}>Добавить</button></div><table><thead><tr><th>#</th><th>Товар</th><th>Кол-во</th><th>Цена</th><th>Сумма</th></tr></thead><tbody>{items.map((x,i)=><tr key={x.id}><td>{i+1}</td><td>{x.name}</td><td>{x.qty}</td><td>{x.price.toFixed(2)}</td><td>{(x.qty*x.price).toFixed(2)}</td></tr>)}</tbody></table></div><aside className="pay"><h2>К оплате</h2><strong>{total.toFixed(2)} <small>сомони</small></strong><p>Позиций: {items.length}</p><button className="cash">Наличные</button><button className="card">Карта</button><button className="qr">QR</button><button className="bonus">Бонусы</button><hr/><button className="finish">Оплатить и завершить</button></aside></section></main>}createRoot(document.getElementById('root')!).render(<App/>);
