const API=import.meta.env.VITE_API_URL??'http://localhost:8080/api/v1';export type PaymentMethod=0|1|2|3|4;export type LookupResult={product:{id:string;sku:string;name:string;unitOfMeasure:string;barcode:string};stock:number;price:number};function token(){return localStorage.getItem('dado_token')}async function request<T>(path:string,init:RequestInit={}){const headers=new Headers(init.headers);headers.set('Content-Type','application/json');const t=token();if(t)headers.set('Authorization',`Bearer ${t}`);const r=await fetch(`${API}${path}`,{...init,headers});if(!r.ok)throw new Error(await r.text()||`HTTP ${r.status}`);return r.status===204?undefined as T:await r.json() as T}export const api={login:(username:string,password:string)=>request<any>('/auth/login',{method:'POST',body:JSON.stringify({username,password})}),createSale:(storeId:string,warehouseId:string,cashRegisterId:string)=>request<any>('/sales',{method:'POST',body:JSON.stringify({storeId,warehouseId,cashRegisterId})}),getSale:(saleId:string)=>request<any>(`/sales/${saleId}`),lookup:(barcode:string,warehouseId:string,storeId:string)=>request<LookupResult>(`/pos/lookup/${encodeURIComponent(barcode)}?warehouseId=${warehouseId}&storeId=${storeId}`),addBarcode:(saleId:string,barcode:string,quantity:number)=>request<any>(`/sales/${saleId}/items/by-barcode`,{method:'POST',body:JSON.stringify({barcode,quantity})}),checkout:(saleId:string)=>request<any>(`/sales/${saleId}/checkout`,{method:'POST'}),createPayment:(saleId:string,method:PaymentMethod,amount:number)=>request<any>(`/sales/${saleId}/payments`,{method:'POST',body:JSON.stringify({method,amount})}),startPayment:(paymentId:string)=>request<any>(`/payments/${paymentId}/start`,{method:'POST'}),paymentStatus:(paymentId:string)=>request<any>(`/payments/${paymentId}/status`),fiscalize:(saleId:string)=>request<any>(`/sales/${saleId}/fiscalization`,{method:'POST'})};
